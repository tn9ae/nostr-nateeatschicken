[info]
relay_url = "wss://relay.nostr.nateeatschicken.xyz/"
name = "NateeatsChicken Relay"
description = "Lockdown mode: verified nostr.nateeatschicken.xyz users and supporters only."

[network]
address = "0.0.0.0"
port = 8080

[options]
reject_future_seconds = 1800

[limits]
messages_per_sec = 10
subscriptions_per_min = 60
max_event_bytes = 65536

[verified_users]
mode = "enabled"
domain_whitelist = ["relay.nostr.nateeatschicken.xyz"]

[authorization]
pubkey_whitelist = [] # populated by relay_lockdown.sh from supporters.txt
